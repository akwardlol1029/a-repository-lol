local char = game.Players.LocalPlayer.Character

--speed = 100 (copy in executor)

local cf = char:GetPivot()

print(char.Humanoid.Health)

local on = false

game.UserInputService.InputBegan:Connect(function(i, e)
    if not e then
        if i.KeyCode == Enum.KeyCode.E then
            cf = char:GetPivot()
            on = not on
            if on then
                while (char.Humanoid.Health > 0) and on do
                    local dT = task.wait()
                    cf = CFrame.new(cf.Position) * workspace.CurrentCamera.CFrame.Rotation
                    cf *= char.Humanoid.MoveDirection * speed
                    char:PivotTo(cf)
                end
            end
        end
    end
end)

game.Players.LocalPlayer.CharacterAdded:Connect(function(c)
    char = c
end)
